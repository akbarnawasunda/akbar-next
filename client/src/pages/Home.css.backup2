/* ============================================================
   HOME — total rewrite
   Design POV: Swiss editorial magazine.
   Prinsip: nomor section raksasa, hero asimetris, katalog
   sebagai list bukan grid, whitespace 2x, garis putih sebagai
   struktur. Tidak ada glow, gradient warna, atau radius.
   ============================================================ */

/* -- LOCAL TOKENS ------------------------------------------ */
.an-site {
  --hx-gutter: clamp(20px, 5vw, 88px);
  --hx-max:    1520px;
  --hx-line:   var(--paper);
}

.an-site *,
.an-site *::before,
.an-site *::after { box-sizing: border-box; }

.an-site {
  background: var(--ink) !important;
  color: var(--paper);
  overflow-x: clip;
}

/* ============================================================
   NAV
   ============================================================ */
.an-site .an-nav {
  position: sticky;
  top: 0;
  z-index: 90;
  display: grid;
  grid-template-columns: auto 1fr auto auto;
  align-items: center;
  gap: clamp(16px, 3vw, 48px);
  height: 72px;
  padding-inline: var(--hx-gutter);
  border-bottom: 1px solid var(--paper) !important;
  background: var(--ink) !important;
  backdrop-filter: none;
  box-shadow: none !important;
}
.an-site .an-nav::after { content: none !important; }

.an-site .an-wordmark {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  font-family: var(--font-mono);
  font-size: 0.68rem;
  font-weight: 600;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--paper);
  white-space: nowrap;
}
.an-site .an-brand-logo {
  width: 30px;
  height: 30px;
  border: 1px solid var(--paper);
  background: var(--paper);
  padding: 4px;
  border-radius: 0;
}

.an-site .an-nav nav {
  display: flex;
  justify-self: end;
  gap: clamp(16px, 2vw, 32px);
}
.an-site .an-nav nav a {
  position: relative;
  padding-block: 6px;
  font-family: var(--font-mono);
  font-size: 0.64rem;
  font-weight: 500;
  letter-spacing: 0.16em;
  color: var(--mute);
  text-transform: uppercase;
  transition: color 180ms ease;
}
.an-site .an-nav nav a:hover { color: var(--acid); }

.an-site .nav-signal {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 14px;
  border: 1px solid var(--paper);
  background: transparent;
  color: var(--paper);
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  transition: background 180ms ease, color 180ms ease;
}
.an-site .nav-signal:hover {
  background: var(--paper);
  color: var(--ink);
}

.an-site .an-menu-toggle {
  display: none;
  padding: 10px 14px;
  border: 1px solid var(--paper);
  background: transparent;
  color: var(--paper);
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}

/* ============================================================
   HERO — asimetris, portrait kecil di kanan bawah
   ============================================================ */
.an-site .an-hero {
  position: relative;
  display: grid;
  grid-template-columns: minmax(0, 1.55fr) minmax(0, 0.85fr);
  align-items: end;
  gap: clamp(24px, 4vw, 72px);
  min-height: calc(100svh - 72px) !important;
  padding: clamp(60px, 8vh, 120px) var(--hx-gutter) clamp(72px, 9vh, 128px) !important;
  border-bottom: 1px solid var(--paper) !important;
  background: var(--ink) !important;
  overflow: hidden;
}
.an-site .an-hero::before,
.an-site .an-hero::after { content: none !important; display: none !important; }

.an-site .home-hero-portrait {
  position: relative !important;
  inset: auto !important;
  grid-column: 2 / 3;
  grid-row: 1 / 2;
  width: 100% !important;
  height: min(72vh, 640px) !important;
  border: 1px solid var(--paper);
  overflow: hidden;
  background: var(--ink-soft);
  align-self: end;
}
.an-site .home-hero-portrait::after { content: none !important; display: none !important; }
.an-site .home-hero-portrait img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: 50% 30%;
  filter: saturate(0.9) contrast(1.05);
}

.an-site .hero-copy {
  position: relative;
  z-index: 2;
  grid-column: 1 / 2;
  grid-row: 1 / 2;
  max-width: 100%;
  padding-inline: 0 !important;
  padding-bottom: clamp(0px, 3vh, 40px);
}

.an-site .hero-copy .eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  margin: 0 0 32px;
  font-family: var(--font-mono);
  font-size: 0.66rem;
  font-weight: 600;
  letter-spacing: 0.22em;
  color: var(--paper);
  text-transform: uppercase;
}
.an-site .hero-copy .eyebrow > span {
  display: block;
  width: 32px;
  height: 1px;
  background: var(--acid);
}

.an-site .hero-copy .hero-title-editorial {
  display: block;
  margin: 0;
  font-family: var(--font-display);
  font-variation-settings: "opsz" 144, "SOFT" 30, "WONK" 1;
  font-weight: 400;
  font-size: clamp(4rem, 11vw, 12rem) !important;
  line-height: 0.82 !important;
  letter-spacing: -0.05em;
  color: var(--paper);
  text-shadow: none !important;
  text-transform: none;
  animation: none !important;
}
.an-site .hero-copy .hero-title-mask {
  display: block;
  overflow: hidden;
  padding: 0 0.02em 0.06em;
}
.an-site .hero-copy .hero-title-word {
  display: inline-block;
  opacity: 0;
  transform: translate3d(0, 105%, 0);
  animation: hero-word-in 1300ms cubic-bezier(0.16, 1, 0.3, 1) calc(120ms + var(--hero-word-index, 0) * 140ms) both;
}
@keyframes hero-word-in {
  to { opacity: 1; transform: none; }
}

.an-site .hero-description {
  max-width: 46ch;
  margin: 34px 0 40px;
  color: var(--mute);
  font-size: 1.06rem;
  line-height: 1.68;
  opacity: 0;
  transform: translate3d(0, 14px, 0);
  animation: hero-support-in 900ms cubic-bezier(0.16, 1, 0.3, 1) 700ms both;
}
.an-site .hero-actions {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 16px 26px;
  opacity: 0;
  transform: translate3d(0, 12px, 0);
  animation: hero-support-in 900ms cubic-bezier(0.16, 1, 0.3, 1) 900ms both;
}
@keyframes hero-support-in {
  to { opacity: 1; transform: none; }
}

/* Buttons */
.an-site .button-primary {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 16px 24px;
  border: 1px solid var(--acid);
  border-radius: 0;
  background: var(--acid);
  color: var(--ink);
  font-family: var(--font-mono);
  font-size: 0.66rem;
  font-weight: 700;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  box-shadow: none;
  transition: background 180ms ease, color 180ms ease, border-color 180ms ease;
}
.an-site .button-primary:hover,
.an-site .button-primary:focus-visible {
  background: var(--paper);
  border-color: var(--paper);
  color: var(--ink);
}
.an-site .button-primary::before { content: none !important; }
.an-site .button-primary:active { transform: scale(0.985); }

.an-site .button-quiet {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 16px 0;
  border: 0;
  border-bottom: 1px solid var(--paper);
  border-radius: 0;
  background: transparent;
  color: var(--paper);
  font-family: var(--font-mono);
  font-size: 0.66rem;
  font-weight: 600;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  transition: border-color 180ms ease, color 180ms ease;
}
.an-site .button-quiet:hover,
.an-site .button-quiet:focus-visible {
  border-color: var(--acid);
  color: var(--acid);
}

.an-site .hero-signal-link {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.16em;
  color: var(--mute);
  text-transform: uppercase;
  transition: color 180ms ease;
}
.an-site .hero-signal-link:hover { color: var(--paper); }

.an-site .hero-scroll-cue { display: none !important; }

/* Kill legacy art */
.an-site .home-hero-atmosphere,
.an-site .home-wordmark-particles,
.an-site .home-ambient-canvas,
.an-site .an-rmx-particle-hero,
.an-site .home-hero-orbit { display: none !important; }

/* ============================================================
   SECTION — base
   ============================================================ */
.an-site .section,
.an-site .home-signal-deck,
.an-site .signal-section {
  position: relative;
  padding-inline: var(--hx-gutter);
  padding-block: clamp(120px, 15vw, 240px);
  background: var(--ink) !important;
  background-image: none !important;
  border-top: 1px solid var(--paper) !important;
}
.an-site .section > * { min-width: 0; }

/* Nomor section raksasa */
.an-section-index {
  display: grid;
  grid-template-columns: auto 1fr;
  align-items: baseline;
  gap: 24px;
  width: min(100%, var(--hx-max));
  margin: 0 auto clamp(56px, 7vw, 100px);
  font-family: var(--font-mono);
  color: var(--acid);
  text-transform: uppercase;
}
.an-section-index > span:first-child {
  display: block !important;
  font-size: clamp(3rem, 7vw, 6.5rem);
  font-weight: 400;
  line-height: 0.9;
  letter-spacing: -0.02em;
  color: var(--acid);
  font-family: var(--font-display);
  font-variation-settings: "opsz" 144, "SOFT" 30, "WONK" 0;
}
.an-section-index > i {
  display: block !important;
  height: 1px;
  background: var(--paper);
  align-self: center;
}
.an-section-index > span:last-child {
  grid-column: 2 / 3;
  font-size: 0.66rem;
  font-weight: 600;
  letter-spacing: 0.22em;
  color: var(--paper);
  margin-top: 12px;
}
.an-section-index::before { content: none !important; }

.an-site .section-heading,
.an-site .section-inline {
  display: block;
  width: min(100%, var(--hx-max));
  margin: 0 auto clamp(64px, 8vw, 120px);
}
.an-site .section-heading::after,
.an-site .section-inline::after { content: none !important; }

.an-site .section h2 {
  max-width: 16ch;
  margin: 0;
  font-family: var(--font-display);
  font-variation-settings: "opsz" 120, "SOFT" 40, "WONK" 1;
  font-weight: 400;
  font-size: clamp(3rem, 7vw, 7.5rem) !important;
  line-height: 0.86;
  letter-spacing: -0.048em;
  text-transform: none;
  color: var(--paper);
}

.an-site .eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  margin: 0 0 20px;
  font-family: var(--font-mono);
  font-size: 0.64rem;
  font-weight: 600;
  letter-spacing: 0.22em;
  color: var(--mute);
  text-transform: uppercase;
}
.an-site .eyebrow > span {
  display: block;
  width: 24px;
  height: 1px;
  background: var(--acid);
}

.an-site .mono-label {
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.18em;
  color: var(--acid);
  text-transform: uppercase;
}

.an-site .text-link,
.an-site .home-deck-cta {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding-bottom: 6px;
  border-bottom: 1px solid var(--paper);
  color: var(--paper);
  font-family: var(--font-mono);
  font-size: 0.64rem;
  font-weight: 600;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  transition: border-color 180ms ease, color 180ms ease;
}
.an-site .text-link:hover,
.an-site .home-deck-cta:hover {
  border-color: var(--acid);
  color: var(--acid);
}

/* ============================================================
   01 — PLATFORMS as vertical list
   ============================================================ */
.an-site .home-signal-deck {
  display: block;
  padding-block: clamp(120px, 14vw, 220px);
}
.an-site .home-signal-deck::before { content: none !important; }

.an-site .home-signal-copy {
  width: min(100%, var(--hx-max));
  margin: 0 auto clamp(64px, 7vw, 112px);
  max-width: 100%;
}
.an-site .home-signal-copy h2 {
  margin: 0 0 28px;
  font-family: var(--font-display);
  font-variation-settings: "opsz" 120, "SOFT" 40, "WONK" 1;
  font-weight: 400;
  font-size: clamp(3rem, 7vw, 7.5rem);
  line-height: 0.86;
  letter-spacing: -0.048em;
  color: var(--paper);
}
.an-site .home-signal-copy > p:not(.eyebrow) {
  max-width: 46ch;
  margin: 0 0 32px;
  color: var(--mute);
  line-height: 1.7;
}

.an-site .home-platform-rack {
  display: block !important;
  width: min(100%, var(--hx-max));
  margin: 0 auto;
  border-top: 1px solid var(--paper);
  border-left: 0 !important;
  background: transparent !important;
}

.an-site .home-platform-card {
  --brand: var(--paper);
  position: relative;
  display: grid !important;
  grid-template-columns: 60px 40px minmax(0, 1fr) auto auto;
  align-items: center;
  gap: 24px;
  min-height: 0 !important;
  padding: 28px 0 !important;
  border: 0 !important;
  border-bottom: 1px solid var(--paper) !important;
  background: transparent !important;
  color: var(--paper);
  transition: padding-left 220ms cubic-bezier(0.16, 1, 0.3, 1), background 180ms ease;
}
.an-site .home-platform-card::before {
  content: none !important;
}
.an-site .home-platform-card::after { content: none !important; }
.an-site .home-platform-card:hover,
.an-site .home-platform-card:focus-visible {
  background: color-mix(in srgb, var(--paper) 4%, transparent) !important;
  padding-left: 20px !important;
  border-color: var(--paper) !important;
  box-shadow: none !important;
  transform: none !important;
}

.home-platform-number {
  display: block !important;
  font-family: var(--font-mono);
  font-size: 0.7rem;
  font-weight: 600;
  letter-spacing: 0.18em;
  color: var(--mute);
  text-transform: uppercase;
}
.home-platform-icon-shell {
  display: grid;
  place-items: center;
  width: 32px;
  height: 32px;
  color: var(--paper);
}
.home-platform-icon-shell svg { width: 28px; height: 28px; }
.home-platform-copy { display: block; min-width: 0; }
.home-platform-copy strong {
  display: block;
  color: var(--paper);
  font-family: var(--font-display);
  font-variation-settings: "opsz" 96, "SOFT" 40, "WONK" 0;
  font-weight: 400;
  font-size: clamp(1.5rem, 2.6vw, 2.4rem);
  letter-spacing: -0.03em;
  line-height: 1;
}
.home-platform-copy small { display: none; }

.home-platform-arrow {
  grid-column: 5 / 6;
  position: static !important;
  color: var(--mute);
  transition: transform 220ms cubic-bezier(0.16, 1, 0.3, 1), color 180ms ease;
}
.an-site .home-platform-card:hover .home-platform-arrow {
  color: var(--acid);
  transform: translateX(4px);
}

.platform-spotify      { --brand: #1ed760; }
.platform-youtube      { --brand: #ff3d3d; }
.platform-soundcloud   { --brand: #ff6d17; }
.platform-instagram    { --brand: #ef6aa8; }
.platform-apple-music  { --brand: #fa57c1; }
.platform-deezer       { --brand: #a238ff; }
.platform-amazon-music { --brand: #ffad36; }
.platform-tidal        { --brand: #1cd6c8; }
.platform-tiktok       { --brand: #31f2e4; }
.platform-x            { --brand: var(--paper); }

.an-platform-marquee { display: none !important; }

/* ============================================================
   02 — CURRENT RELEASE
   ============================================================ */
.an-site .section-current {
  background: var(--ink) !important;
  background-image: none !important;
}
.an-site .feature-release {
  display: grid;
  grid-template-columns: minmax(320px, 0.85fr) minmax(0, 1.15fr);
  align-items: end;
  gap: clamp(48px, 8vw, 140px);
  width: min(100%, var(--hx-max));
  margin: 0 auto;
  position: relative;
}
.an-site .feature-release::before { content: none !important; }

.an-site .release-cover {
  position: relative;
  width: 100%;
  aspect-ratio: 1 / 1;
  border: 1px solid var(--paper);
  background: var(--ink-soft);
  overflow: hidden;
  margin: 0;
}
.an-site .release-cover img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 0;
  filter: saturate(0.95) contrast(1.04);
}
.an-site .release-cover::after { content: none !important; }
.an-site .cover-orbit { display: none !important; }

.an-site .release-detail { max-width: 100%; }
.an-site .release-detail h3 {
  margin: 20px 0 24px;
  font-family: var(--font-display);
  font-variation-settings: "opsz" 120, "SOFT" 40, "WONK" 1;
  font-weight: 400;
  font-size: clamp(2.6rem, 6vw, 6rem);
  line-height: 0.88;
  letter-spacing: -0.045em;
  color: var(--paper);
}
.an-site .release-detail > p:not(.mono-label) {
  max-width: 48ch;
  color: var(--mute);
  font-size: 1.02rem;
  line-height: 1.72;
}
.an-site .release-detail-actions {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20px;
  margin-top: 40px;
  padding-top: 22px;
  border-top: 1px solid var(--paper);
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.16em;
  color: var(--mute);
  text-transform: uppercase;
}

/* ============================================================
   05 — CATALOG as list rows (bukan grid)
   ============================================================ */
.an-site .release-section { background: var(--ink) !important; background-image: none !important; }

.an-site .release-grid {
  display: block !important;
  width: min(100%, var(--hx-max));
  margin: 0 auto;
  border-top: 1px solid var(--paper);
  border-left: 0 !important;
  border-right: 0 !important;
  border-bottom: 0 !important;
  gap: 0 !important;
  transform: none !important;
}
.an-site .release-grid > * { min-width: 0; }

.an-site .release-card {
  display: grid !important;
  grid-template-columns: 100px 96px minmax(0, 1fr) auto;
  align-items: center !important;
  gap: 28px !important;
  min-height: 0 !important;
  padding: 26px 0 !important;
  border: 0 !important;
  border-bottom: 1px solid var(--paper) !important;
  background: transparent !important;
  color: var(--paper);
  opacity: 1 !important;
  transform: none !important;
  animation: none !important;
  transition: padding-left 220ms cubic-bezier(0.16, 1, 0.3, 1), background 180ms ease;
}
.an-site .release-card:hover,
.an-site .release-card:focus-visible {
  background: color-mix(in srgb, var(--paper) 4%, transparent);
  padding-left: 20px !important;
  border-color: var(--paper) !important;
  box-shadow: none !important;
  transform: none !important;
}
.an-site .release-card::before { content: none !important; }

.an-site .release-card-art {
  width: 96px;
  height: 96px;
  margin: 0;
  border: 1px solid var(--paper);
  background: var(--ink-soft);
  overflow: hidden;
  order: 2;
}
.an-site .release-card-art img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  filter: saturate(0.95) contrast(1.04);
  transition: filter 220ms ease;
}
.an-site .release-card:hover .release-card-art img { filter: saturate(1.05) contrast(1.04); }

.an-site .release-card > svg {
  grid-column: 2 / 3;
  grid-row: 1 / 2;
  color: var(--paper);
  order: 1;
  width: 24px;
  height: 24px;
  justify-self: center;
}
.an-site .release-card p {
  grid-column: 1 / 2;
  grid-row: 1 / 2;
  margin: 0;
  order: 0;
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.16em;
  color: var(--mute);
  text-transform: uppercase;
}
.an-site .release-card h3 {
  grid-column: 3 / 4;
  grid-row: 1 / 2;
  margin: 0;
  font-family: var(--font-display);
  font-variation-settings: "opsz" 96, "SOFT" 40, "WONK" 0;
  font-weight: 400;
  font-size: clamp(1.5rem, 2.8vw, 2.6rem);
  line-height: 1;
  letter-spacing: -0.03em;
  color: var(--paper);
}
.an-site .release-platform-line {
  grid-column: 4 / 5;
  grid-row: 1 / 2;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin: 0;
  padding: 0;
  font-family: var(--font-mono);
  font-size: 0.6rem;
  font-weight: 600;
  letter-spacing: 0.16em;
  color: var(--acid);
  text-transform: uppercase;
}

/* ============================================================
   06 — VIDEO
   ============================================================ */
.an-site .visual-section { background: var(--ink) !important; background-image: none !important; }

.an-site .video-grid {
  display: grid;
  grid-template-columns: 1.6fr 1fr;
  grid-template-rows: repeat(2, minmax(240px, 1fr));
  gap: 16px;
  width: min(100%, var(--hx-max));
  margin: 0 auto;
  min-height: 620px;
}
.an-site .video-card {
  position: relative;
  overflow: hidden;
  border: 1px solid var(--paper);
  background: var(--ink-soft);
  color: var(--paper);
  transition: border-color 180ms ease;
}
.an-site .video-card:first-child { grid-row: 1 / -1; }
.an-site .video-card:hover { border-color: var(--acid); }
.an-site .video-card > img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  filter: saturate(0.85) contrast(1.04) brightness(0.92);
  transition: filter 300ms ease;
}
.an-site .video-card:hover > img { filter: saturate(1) contrast(1.05) brightness(0.98); }
.an-site .video-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(0deg, color-mix(in srgb, var(--ink) 92%, transparent), transparent 62%);
  pointer-events: none;
}
.an-site .video-content {
  position: absolute;
  inset: auto 26px 26px;
  z-index: 1;
  display: grid;
  gap: 12px;
}
.an-site .video-content > span {
  font-family: var(--font-mono);
  font-size: 0.6rem;
  font-weight: 600;
  letter-spacing: 0.16em;
  color: var(--acid);
  text-transform: uppercase;
}
.an-site .video-content h3 {
  max-width: 22ch;
  margin: 0;
  font-family: var(--font-display);
  font-variation-settings: "opsz" 96, "SOFT" 40, "WONK" 0;
  font-weight: 400;
  font-size: clamp(1.3rem, 2.2vw, 2rem);
  line-height: 1.02;
  letter-spacing: -0.03em;
  color: var(--paper);
}
.an-site .round-play {
  display: grid;
  place-items: center;
  width: 52px;
  height: 52px;
  margin-top: 10px;
  border: 1px solid var(--paper);
  border-radius: 0;
  background: transparent;
  color: var(--paper);
  transition: background 180ms ease, color 180ms ease, border-color 180ms ease;
}
.an-site .video-card:hover .round-play {
  background: var(--acid);
  color: var(--ink);
  border-color: var(--acid);
}

/* ============================================================
   07 — LIVE
   ============================================================ */
.an-site .live-section {
  position: relative;
  display: grid;
  grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
  gap: clamp(48px, 7vw, 120px);
  align-items: start;
  min-height: 0;
  padding-block: clamp(120px, 14vw, 200px);
  border-top: 1px solid var(--paper);
  background: var(--ink) !important;
  background-image: none !important;
  overflow: hidden;
}
.an-site .live-section::after { content: none !important; }

.an-site .live-backdrop { display: none !important; }

.an-site .live-copy {
  position: relative;
  z-index: 1;
  max-width: 100%;
}
.an-site .live-copy h2 {
  margin: 20px 0 28px;
  font-family: var(--font-display);
  font-variation-settings: "opsz" 120, "SOFT" 40, "WONK" 1;
  font-weight: 400;
  font-size: clamp(3rem, 7vw, 7rem);
  line-height: 0.86;
  letter-spacing: -0.048em;
  color: var(--paper);
}
.an-site .live-copy > p:not(.eyebrow) {
  max-width: 46ch;
  margin: 0 0 36px;
  color: var(--mute);
  line-height: 1.7;
}

.an-site .live-status {
  position: relative;
  z-index: 1;
  align-self: start;
  padding: 32px;
  border: 1px solid var(--paper);
  background: transparent;
}
.an-site .live-status > span {
  display: block;
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.18em;
  color: var(--acid);
  text-transform: uppercase;
}
.an-site .live-status strong {
  display: block;
  margin: 18px 0 24px;
  font-family: var(--font-display);
  font-variation-settings: "opsz" 96, "SOFT" 40, "WONK" 0;
  font-weight: 400;
  font-size: clamp(1.6rem, 3vw, 2.6rem);
  line-height: 1;
  letter-spacing: -0.03em;
  color: var(--paper);
}
.an-site .live-status small {
  display: block;
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.16em;
  color: var(--mute);
  text-transform: uppercase;
}

.an-site .home-live-events {
  grid-column: 1 / -1;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 0;
  margin-top: 40px;
  border-top: 1px solid var(--paper);
  border-left: 1px solid var(--paper);
}
.an-site .home-live-event {
  position: relative;
  display: flex;
  flex-direction: column;
  min-height: 190px;
  padding: 28px;
  border: 0;
  border-right: 1px solid var(--paper);
  border-bottom: 1px solid var(--paper);
  background: transparent;
  transition: background 180ms ease;
}
.an-site .home-live-event:hover { background: color-mix(in srgb, var(--paper) 4%, transparent); }
.an-site .home-live-event > span {
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.18em;
  color: var(--acid);
  text-transform: uppercase;
}
.an-site .home-live-event strong {
  margin: 20px 0 14px;
  font-family: var(--font-display);
  font-variation-settings: "opsz" 96, "SOFT" 40, "WONK" 0;
  font-weight: 400;
  font-size: clamp(1.3rem, 2vw, 1.8rem);
  line-height: 1.02;
  letter-spacing: -0.03em;
  color: var(--paper);
}
.an-site .home-live-event small {
  margin-top: auto;
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 500;
  letter-spacing: 0.1em;
  color: var(--mute);
  line-height: 1.5;
}
.an-site .home-live-event-source {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  margin-top: 18px;
  font-family: var(--font-mono);
  font-size: 0.6rem;
  font-weight: 600;
  letter-spacing: 0.16em;
  color: var(--paper);
  text-transform: uppercase;
  transition: color 180ms ease;
}
.an-site .home-live-event-source:hover { color: var(--acid); }

.an-site .an-event-location-link,
.an-site .en-event-location-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  color: inherit;
}
.an-site .an-event-location-link:hover,
.an-site .en-event-location-link:hover {
  color: var(--acid);
  text-decoration: underline;
  text-underline-offset: 3px;
}

/* ============================================================
   08 — GAME TEASER
   ============================================================ */
.an-site .game-teaser-section {
  position: relative;
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
  align-items: center;
  gap: clamp(40px, 8vw, 140px);
  padding: clamp(120px, 14vw, 200px) var(--hx-gutter);
  border-top: 1px solid var(--paper);
  background: var(--ink) !important;
  background-image: none !important;
  overflow: hidden;
}
.an-site .game-teaser-section::before { content: none !important; }
.an-site .game-teaser-section > .an-section-index {
  grid-column: 1 / -1;
}

.an-site .game-teaser-art {
  position: relative;
  order: 2;
  min-height: 440px;
  border: 1px solid var(--paper);
  background: var(--ink-soft);
  overflow: hidden;
  isolation: isolate;
}
.an-site .game-teaser-scanline {
  position: absolute;
  inset: 0;
  background: repeating-linear-gradient(180deg, rgba(240, 235, 220, 0.05) 0 1px, transparent 1px 5px);
  opacity: 0.6;
  pointer-events: none;
  z-index: 4;
}
.an-site .game-teaser-sun {
  position: absolute;
  top: 24%;
  right: 16%;
  width: 96px;
  height: 96px;
  border-radius: 0;
  background: var(--signal);
}
.an-site .game-teaser-mountain {
  position: absolute;
  right: -10%;
  bottom: 27%;
  width: 120%;
  height: 44%;
  background: #2a2622;
  clip-path: polygon(0 100%, 16% 51%, 25% 70%, 39% 14%, 50% 65%, 66% 31%, 77% 68%, 88% 23%, 100% 73%, 100% 100%);
}
.an-site .game-teaser-mountain-b {
  right: -2%;
  bottom: 22%;
  height: 30%;
  background: #1a1816;
}
.an-site .game-teaser-gate {
  position: absolute;
  right: 18%;
  bottom: 26%;
  width: 42px;
  height: 82px;
  border: 2px solid var(--signal);
  background: var(--ink);
  z-index: 2;
}
.an-site .game-teaser-runner {
  position: absolute;
  bottom: 26%;
  left: 18%;
  display: grid;
  place-items: center;
  width: 44px;
  height: 68px;
  border: 2px solid var(--acid);
  border-radius: 0;
  background: var(--ink);
  color: var(--acid);
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 700;
  z-index: 3;
  transform: none;
}
.an-site .game-teaser-runner::before {
  content: "";
  position: absolute;
  top: -18px;
  width: 26px;
  height: 26px;
  border: 2px solid var(--paper);
  border-radius: 50%;
  background: var(--ink);
}
.an-site .game-teaser-note {
  position: absolute;
  width: 18px;
  height: 18px;
  border: 2px solid var(--acid);
  border-radius: 50%;
  z-index: 3;
}
.an-site .game-teaser-note::after {
  content: "";
  position: absolute;
  inset: 4px;
  border: 1px solid var(--acid);
  border-radius: 50%;
}
.an-site .game-teaser-note-a { bottom: 55%; left: 42%; }
.an-site .game-teaser-note-b { right: 31%; bottom: 65%; }
.an-site .game-teaser-signal-line {
  position: absolute;
  right: 0;
  bottom: 25%;
  left: 0;
  height: 2px;
  background: var(--acid);
  z-index: 3;
}
.an-site .game-teaser-score { display: none !important; }

.an-site .game-teaser-copy { order: 1; position: relative; z-index: 2; }
.an-site .game-teaser-copy h2 {
  margin: 20px 0 24px;
  font-family: var(--font-display);
  font-variation-settings: "opsz" 120, "SOFT" 40, "WONK" 1;
  font-weight: 400;
  font-size: clamp(2.8rem, 6vw, 6rem);
  line-height: 0.86;
  letter-spacing: -0.048em;
  color: var(--paper);
}
.an-site .game-teaser-copy > p:not(.eyebrow) {
  max-width: 44ch;
  margin: 0 0 32px;
  color: var(--mute);
  line-height: 1.7;
}

/* ============================================================
   09 — SIGNAL (paper, acid panel)
   ============================================================ */
.an-site .signal-section {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(360px, 0.85fr);
  align-items: end;
  gap: clamp(48px, 8vw, 140px);
  padding-block: clamp(120px, 14vw, 200px);
  border-top: 1px solid var(--paper);
  background: var(--paper) !important;
  background-image: none !important;
  color: var(--ink) !important;
}
.an-site .signal-section::before { content: none !important; }
.an-site .signal-section > .an-section-index {
  grid-column: 1 / -1;
  color: var(--ink);
}
.an-site .signal-section > .an-section-index > span:first-child { color: var(--ink); }
.an-site .signal-section > .an-section-index > i { background: var(--ink); }
.an-site .signal-section > .an-section-index > span:last-child { color: var(--ink); }

.an-site .signal-section .eyebrow {
  color: var(--ink);
}
.an-site .signal-section .eyebrow > span { background: var(--ink); }

.an-site .signal-section h2 {
  margin: 20px 0 24px;
  font-family: var(--font-display);
  font-variation-settings: "opsz" 120, "SOFT" 40, "WONK" 1;
  font-weight: 400;
  font-size: clamp(3rem, 7vw, 7rem);
  line-height: 0.86;
  letter-spacing: -0.048em;
  color: var(--ink);
}
.an-site .signal-section > div > p:not(.eyebrow) {
  max-width: 44ch;
  color: color-mix(in srgb, var(--ink) 78%, transparent);
  line-height: 1.65;
}

.an-site .signal-section .nf-signal-form {
  width: 100%;
  max-width: 520px;
  justify-self: end;
}
.an-site .signal-section .nf-signal-form label {
  display: block;
  margin-bottom: 12px;
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.18em;
  color: var(--ink);
  text-transform: uppercase;
}
.an-site .signal-section .nf-signal-form > div {
  display: flex;
  align-items: center;
  gap: 0;
  padding: 0;
  border: 1px solid var(--ink);
  background: var(--paper);
}
.an-site .signal-section .nf-signal-form > div > svg { display: none; }
.an-site .signal-section .nf-signal-form input {
  flex: 1;
  min-width: 0;
  padding: 18px 20px;
  border: 0;
  background: transparent;
  color: var(--ink);
  font-family: var(--font-body);
  font-size: 1rem;
  outline: none;
}
.an-site .signal-section .nf-signal-form input::placeholder {
  color: color-mix(in srgb, var(--ink) 45%, transparent);
}
.an-site .signal-section .nf-signal-form button {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 18px 24px;
  border: 0;
  border-left: 1px solid var(--ink);
  background: var(--ink);
  color: var(--acid);
  font-family: var(--font-mono);
  font-size: 0.64rem;
  font-weight: 700;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}
.an-site .signal-section .nf-signal-form button:disabled { opacity: 0.5; cursor: not-allowed; }
.an-site .signal-section .nf-signal-form small {
  display: block;
  margin-top: 16px;
  color: color-mix(in srgb, var(--ink) 62%, transparent);
  font-size: 0.78rem;
  line-height: 1.55;
}

/* ============================================================
   FOOTER — nama raksasa sebagai signature
   ============================================================ */
.an-site .an-footer {
  display: grid;
  grid-template-columns: 1.6fr 1fr 1fr;
  gap: clamp(40px, 6vw, 96px);
  padding: clamp(88px, 10vw, 140px) var(--hx-gutter) 34px;
  border-top: 1px solid var(--paper);
  background: var(--ink) !important;
  color: var(--mute) !important;
}
.an-site .an-footer::before { content: none !important; }

.an-site .footer-brand strong {
  display: block;
  margin: 20px 0 0;
  font-family: var(--font-display);
  font-variation-settings: "opsz" 96, "SOFT" 40, "WONK" 0;
  font-weight: 400;
  font-size: clamp(2rem, 4vw, 3.4rem);
  letter-spacing: -0.04em;
  line-height: 0.92;
  color: var(--paper);
}
.an-site .footer-brand p {
  margin: 12px 0 0;
  font-family: var(--font-mono);
  font-size: 0.62rem;
  font-weight: 600;
  letter-spacing: 0.18em;
  text-transform: uppercase;
}
.an-site .an-mark {
  display: inline-grid;
  place-items: center;
  width: 44px;
  height: 44px;
  border: 1px solid var(--paper);
  font-family: var(--font-mono);
  font-size: 0.66rem;
  font-weight: 600;
  letter-spacing: 0.14em;
  color: var(--paper);
}

.an-site .footer-links {
  display: flex;
  flex-direction: column;
  gap: 14px;
}
.an-site .footer-links > span {
  font-family: var(--font-mono);
  font-size: 0.6rem;
  font-weight: 600;
  letter-spacing: 0.18em;
  color: var(--mute-soft);
  text-transform: uppercase;
}
.an-site .footer-links a {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  color: var(--mute);
  font-size: 0.95rem;
  transition: color 180ms ease;
}
.an-site .footer-links a:hover { color: var(--acid); }

.an-site .footer-bottom {
  grid-column: 1 / -1;
  margin: 60px 0 0;
  padding-top: 24px;
  border-top: 1px solid var(--paper);
  font-family: var(--font-mono);
  font-size: 0.6rem;
  font-weight: 600;
  letter-spacing: 0.18em;
  color: var(--mute-soft);
  text-transform: uppercase;
}

/* ============================================================
   MOBILE
   ============================================================ */
@media (max-width: 1024px) {
  .an-site .feature-release {
    grid-template-columns: minmax(260px, 0.9fr) minmax(0, 1.1fr);
    gap: 48px;
  }
}

@media (max-width: 820px) {
  .an-site {
    --hx-gutter: 20px;
  }

  .an-site .an-nav {
    grid-template-columns: auto 1fr auto;
    height: 60px;
    gap: 12px;
  }
  .an-site .an-nav nav { display: none; }
  .an-site .nav-signal { font-size: 0; padding: 10px; gap: 0; }
  .an-site .an-menu-toggle { display: inline-grid; }
  .an-site .an-wordmark > span { font-size: 0.62rem; }

  .an-site .an-hero {
    grid-template-columns: 1fr;
    min-height: 0 !important;
    padding-block: 40px 64px !important;
    gap: 32px;
  }
  .an-site .home-hero-portrait {
    grid-column: 1 / 2;
    grid-row: 1 / 2;
    width: min(100%, 480px) !important;
    height: clamp(360px, 96vw, 500px) !important;
    justify-self: start;
  }
  .an-site .hero-copy {
    grid-column: 1 / 2;
    grid-row: 2 / 3;
    padding-bottom: 0;
  }
  .an-site .hero-copy .hero-title-editorial {
    font-size: clamp(3.4rem, 15vw, 5.6rem) !important;
  }
  .an-site .hero-description {
    font-size: 1rem;
    margin: 26px 0 32px;
  }

  .an-site .section,
  .an-site .home-signal-deck,
  .an-site .live-section,
  .an-site .signal-section,
  .an-site .game-teaser-section {
    padding-block: clamp(80px, 14vw, 140px);
  }

  .an-section-index {
    grid-template-columns: 1fr;
    gap: 12px;
    margin-bottom: 40px;
  }
  .an-section-index > span:first-child { font-size: clamp(2.4rem, 12vw, 4rem); }
  .an-section-index > i { display: none !important; }
  .an-section-index > span:last-child { grid-column: 1 / 2; margin-top: 0; }

  .an-site .section h2,
  .an-site .home-signal-copy h2,
  .an-site .live-copy h2,
  .an-site .game-teaser-copy h2,
  .an-site .signal-section h2 {
    font-size: clamp(2.6rem, 12vw, 4.4rem) !important;
  }

  .an-site .home-platform-card {
    grid-template-columns: 40px 32px minmax(0, 1fr) auto;
    gap: 14px;
    padding: 22px 0 !important;
  }
  .an-site .home-platform-copy strong {
    font-size: clamp(1.3rem, 5vw, 1.7rem);
  }
  .an-site .home-platform-card:hover { padding-left: 8px !important; }

  .an-site .feature-release {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  .an-site .release-cover { width: min(100%, 380px); }
  .an-site .release-detail h3 { font-size: clamp(2rem, 10vw, 3.4rem); }

  .an-site .release-card {
    grid-template-columns: 1fr;
    grid-template-rows: auto auto auto;
    gap: 16px !important;
    padding: 24px 0 !important;
  }
  .an-site .release-card p {
    grid-column: 1 / 2;
    grid-row: 1 / 2;
    order: 0;
  }
  .an-site .release-card-art {
    width: 100%;
    height: auto;
    aspect-ratio: 4 / 3;
    order: 2;
    grid-column: 1 / 2;
    grid-row: 3 / 4;
    margin-top: 16px;
  }
  .an-site .release-card > svg {
    display: none !important;
  }
  .an-site .release-card h3 {
    grid-column: 1 / 2;
    grid-row: 2 / 3;
    font-size: clamp(1.5rem, 6vw, 2rem);
  }
  .an-site .release-platform-line {
    grid-column: 1 / 2;
    grid-row: 4 / 5;
    justify-self: start;
  }

  .an-site .video-grid {
    grid-template-columns: 1fr;
    grid-template-rows: auto;
    min-height: 0;
    gap: 14px;
  }
  .an-site .video-card:first-child { grid-row: auto; }
  .an-site .video-card { min-height: 280px; }

  .an-site .live-section {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  .an-site .home-live-events {
    grid-template-columns: 1fr;
  }
  .an-site .home-live-event { border-right: 0; }

  .an-site .game-teaser-section {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  .an-site .game-teaser-art { order: 1; min-height: 320px; }
  .an-site .game-teaser-copy { order: 2; }

  .an-site .signal-section {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  .an-site .signal-section .nf-signal-form { justify-self: stretch; }
  .an-site .signal-section .nf-signal-form > div { flex-wrap: wrap; }
  .an-site .signal-section .nf-signal-form button {
    width: 100%;
    justify-content: center;
    border-left: 0;
    border-top: 1px solid var(--ink);
  }

  .an-site .an-footer {
    grid-template-columns: 1fr 1fr;
    gap: 40px;
  }
  .an-site .footer-brand,
  .an-site .footer-bottom { grid-column: 1 / -1; }
}

@media (max-width: 480px) {
  .an-site .hero-copy .hero-title-editorial {
    font-size: clamp(3rem, 16vw, 4.6rem) !important;
  }
  .an-site .home-platform-card {
    grid-template-columns: 32px 28px minmax(0, 1fr) auto;
    gap: 12px;
  }
  .an-site .an-footer { grid-template-columns: 1fr; }
  .an-site .footer-brand { grid-column: auto; }
  .an-site .hero-actions { flex-direction: column; align-items: stretch; gap: 16px; }
  .an-site .button-primary { justify-content: center; }
  .an-site .button-quiet { align-self: flex-start; }
}

/* ============================================================
   REDUCED MOTION
   ============================================================ */
@media (prefers-reduced-motion: reduce) {
  .an-site .hero-copy .hero-title-word,
  .an-site .hero-description,
  .an-site .hero-actions {
    opacity: 1;
    transform: none;
    animation: none !important;
  }
  .an-site .home-platform-card,
  .an-site .release-card,
  .an-site .video-card,
  .an-site .home-live-event {
    transition: none !important;
  }
}
